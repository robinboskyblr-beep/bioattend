# ── Core API ──────────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Computer Vision ────────────────────────────────────────────────────────
opencv-python-headless==4.9.0.80
numpy>=1.26.4,<2.0.0

# ── Firebase ───────────────────────────────────────────────────────────────
firebase-admin==7.4.0

# ── Face Recognition — Tier 1 (free tier, ~150MB RAM) ─────────────────────
# MediaPipe: Google's lightweight face mesh — 468 landmarks, runs on CPU.
# Used as primary recognizer when TensorFlow/DeepFace is not available.
mediapipe>=0.10.14,<1.0.0

# ── Face Recognition — Tier 2 (paid plan ≥1GB RAM only) ──────────────────
# To enable ArcFace on paid Render plan: add deepface==0.0.93 tf-keras==2.19.0 tensorflow-cpu==2.19.0
