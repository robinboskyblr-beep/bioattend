# ── Core API ──────────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Computer Vision ────────────────────────────────────────────────────────
opencv-python-headless==4.9.0.80
numpy>=1.26.4,<2.0.0

# ── Firebase ───────────────────────────────────────────────────────────────
firebase-admin==7.4.0

# ── Face Recognition — Tier 1 (InsightFace, ~150MB RAM, no TensorFlow) ────
# Real ArcFace 512-d embeddings via ONNX Runtime — best accuracy on free tier
insightface>=0.7.3
onnxruntime>=1.16.0

# ── Face Recognition — Tier 2 fallback (~100MB RAM) ──────────────────────
mediapipe>=0.10.14,<1.0.0

# ── Face Recognition — Tier 3 (paid plan ≥1GB RAM, max accuracy) ──────────
# To enable DeepFace ArcFace on Render paid plan: deepface==0.0.93 tf-keras==2.19.0 tensorflow-cpu==2.19.0
